// função
function soma(x, y) {
    return x + y;
}
soma(2, 3);
// agrupamento de dados, retorna a soma com base no x + y.

// acao no navegador
alert('alou');
// exibe uma mensagem na tela .

//objeto
const pessoa= {
    nome: 'mayk',
    idade: 25,
};
// objeto que agrupa várias informações relacionadas a uma mesma entidade.

//array
const numeros= [1, 2, 3, 4, 5];
// estrutura que agrupa vários valores em uma única variável.